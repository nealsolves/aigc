policy_version: "1.0"
roles: []

pre_conditions:
  required:
    role_declared:
      type: "boolean"
    schema_exists:
      type: "boolean"

output_schema:
  $schema: "http://json-schema.org/draft-07/schema#"
  type: object
  properties:
    result:
      type: string
