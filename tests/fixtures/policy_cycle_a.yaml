policy_version: "1.0"
description: "Policy A that extends B (creates cycle)"
extends: "policy_cycle_b.yaml"

roles:
  - "cycle_a_role"

pre_conditions:
  required:
    - "context_valid"

post_conditions:
  required:
    - "output_schema_valid"

output_schema:
  type: "object"
  properties:
    result:
      type: "string"
  required:
    - "result"
